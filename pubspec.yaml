name: expense_tracker
description: "A premium personal expense tracking application."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  dio: ^5.4.0
  go_router: ^13.2.0
  get_it: ^7.6.0
  equatable: ^2.0.5
  json_annotation: ^4.12.0
  intl: ^0.19.0
  flutter_screenutil: ^5.9.0
  fl_chart: ^0.69.0
  flutter_animate: ^4.5.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.8
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
